// Filesystem operation defaults.
export const META_CACHE_LIMIT = 60000;
// Keep a bounded in-memory directory cache to avoid unbounded growth.
export const DIR_CACHE_LIMIT = 120;
export const META_FLUSH_INTERVAL_MS = 220;
export const FAST_SORT_THRESHOLD = 800;
export const UNDO_STACK_LIMIT = 20;

export const THUMB_TYPING_PAUSE_MS = 320;
export const THUMB_INTERACTION_COOLDOWN_MS = 180;
