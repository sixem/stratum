// Performance- and timing-related defaults shared by hooks.
export const WINDOW_STORAGE_KEY = "stratum.window";
export const WINDOW_STORAGE_VERSION = 1;
export const WINDOW_PERSIST_DELAY_MS = 240;
export const WINDOW_MIN_SIZE = 320;
export const WINDOW_MAX_SIZE = 8192;

export const DEFAULT_SNAP_PX = 8;
export const LEGACY_TICK_DELTA = 120;
export const LINE_TICK_DELTA = 3;
export const PAGE_TICK_DELTA = 1;
export const PIXEL_TICK_DELTA = 100;

export const DEFAULT_TYPING_RESET_MS = 320;
export const TYPEAHEAD_RESET_MS = 700;

export const THUMB_REQUEST_DELAY_MS = 80;
export const META_REQUEST_DELAY_MS = 70;
export const PREFETCH_INTERVAL_MS = 140;
export const SETTLE_MS = 160;
export const THUMB_EVENT_FLUSH_MS = 80;

export const DIR_CHANGE_DEBOUNCE_MS = 260;
export const TAB_SWITCH_CHECK_DELAY_MS = 140;
export const TAB_SWITCH_REFRESH_COOLDOWN_MS = 2000;
export const REFRESH_COOLDOWN_MS = 900;

export const ENTRY_FADE_IN_MS = 180;
export const ENTRY_FADE_OUT_MS = 180;

export const MAX_SCROLL_RESTORE_ATTEMPTS = 6;
